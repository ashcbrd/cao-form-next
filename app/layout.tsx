import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import "./globals.css"

export const metadata: Metadata = {
  title: "SUGB - Standard Inquiry for Equitable Pay",
  description:
    "Comprehensive pay equity analysis platform with Caoloon integration for organizations to conduct standardized pay equity assessments.",
  keywords: ["pay equity", "compensation analysis", "SUGB", "Caoloon", "HR analytics"],
  authors: [{ name: "SUGB Development Team" }],
  creator: "SUGB Platform",
  publisher: "SUGB Platform",
  robots: "index, follow",
  viewport: "width=device-width, initial-scale=1",
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: "white" },
    { media: "(prefers-color-scheme: dark)", color: "black" },
  ],
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <head>
        <style>{`
html {
  font-family: ${GeistSans.style.fontFamily};
  --font-sans: ${GeistSans.variable};
  --font-mono: ${GeistMono.variable};
}
        `}</style>
      </head>
      <body>{children}</body>
    </html>
  )
}
